@model Synthesizer

<section>
    <h3>@Model.Name</h3>
    <div>
        Type Of synthesizer:@Model.SynthType
    </div>
    <div>
        <a asp-action="Details" asp-route-id="@Model.Id">More</a>
        <a asp-page="/Synthesizers/Edit" asp-route-id="@Model.Id">Edit</a>
    </div>
</section>
